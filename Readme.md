1. Клонирование репозитория
   bash
   git clone <repository-url>
   cd <project-name>
2. Запуск через Docker
   bash

# Полный запуск (сборка + БД + миграции + приложение)

make dev-up

# Просмотр логов

make docker-logs
Приложение будет доступно по адресу: http://localhost:8081
Swagger документация: http://localhost:8081/swagger/index.html

3. Остановка
   bash

# Остановить контейнеры

make dev-down

# Полная очистка (удалить volumes)

make docker-down-clean
🛠️ Команды Makefile
Docker команды
Команда Описание
make docker-up Запустить контейнеры
make docker-up-build Пересобрать и запустить
make docker-down Остановить контейнеры
make docker-down-clean Остановить и удалить volumes
make docker-logs Просмотр всех логов
make docker-logs-app Логи только приложения
make docker-exec Войти в контейнер приложения
make docker-exec-postgres Войти в PostgreSQL
Миграции
Команда Описание
make docker-migrate-up Применить все миграции
make docker-migrate-down Откатить последнюю миграцию
make docker-migrate-version Показать текущую версию
make docker-migrate-force VERSION=xxx Принудительно установить версию
Локальная разработка
Команда Описание
make run Запустить приложение локально
make gen-docs Сгенерировать Swagger документацию
🗄️ База данных
Подключение к БД:

bash

# Через Docker

make docker-exec-postgres

# Локально (если порт проброшен)

psql -h localhost -p 5433 -U postgres -d testingtask
🚨 Возможные проблемы
Порты заняты
bash

# Измените порты в docker-compose.yml или остановите другие контейнеры

Миграции не применяются
bash

# Проверьте версию

make docker-migrate-version

# Принудительный сброс

make docker-migrate-force VERSION=0
make docker-migrate-up
Приложение не отвечает
bash

# Проверьте логи

make docker-logs-app
